#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_configure override_dh_auto_build override_dh_auto_install override_dh_auto_clean:
	true

override_dh_install:
	dh_install
	install -m755 cleverdb-agent.py debian/cleverdb-agent/usr/bin/cleverdb-agent
